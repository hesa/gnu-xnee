
pnee:
  point to cnee if pnee user want more functionality
  
  handle that user can start replay before recording is finished

  make sure "stop" interrupts replay

  why doesn't pnee load the correct file after pne replay?
    - it does after record!

libxnee:

doc:
	build doc by default    

